Original code comes from:
https://github.com/nicolargo/glances/releases/tag/v3.1.4

This version of glances is the same as v3.1.4, with the only modification being that the timestamps exported to csv files are in EPOCH time format (instead of date and time).
